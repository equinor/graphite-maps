# Agent Instructions

This project uses [uv](https://docs.astral.sh/uv/) as its package manager. Do not use `pip`.

- `uv run <command>` — run scripts/tools (e.g., `uv run pytest`)
- `uv add <package>` / `uv remove <package>` — manage dependencies
- `uv sync` — install all dependencies from the lockfile
